<?php

use yii\helpers\Html;
use yii\grid\GridView;
use yii\widgets\DetailView;
use yii\widgets\Pjax;

/**
 * @var yii\web\View $this
 * @var app\models\TipoDado $model
 */
$this->title = 'Tipo de Dado ' . $model->label . '';
$this->params['breadcrumbs'][] = ['label' => 'Tipos de Dado', 'url' => ['index']];
$this->params['breadcrumbs'][] = ['label' => (string) $model->label, 'url' => ['view', 'idTipoDado' => $model->idTipoDado]];
$this->params['breadcrumbs'][] = 'Detalhes';
?>
<div class="tipo-dado-view">

    <p class='pull-left'>
        <?= Html::a('<span class="glyphicon glyphicon-pencil"></span> Editar', ['update', 'idTipoDado' => $model->idTipoDado], ['class' => 'btn btn-info'])
        ?>
        <?= Html::a('<span class="glyphicon glyphicon-plus"></span> Novo Tipo de Dado', ['create'], ['class' => 'btn
        btn-success']) ?>
    </p>

    <p class='pull-right'>
        <?= Html::a('<span class="glyphicon glyphicon-list"></span> Lista', ['index'], ['class' => 'btn btn-default']) ?>
    </p><div class='clearfix'></div> 


    <h3>
        <?= $model->label ?>    </h3>


    <?php $this->beginBlock('app\models\TipoDado'); ?>

    <?php
    echo DetailView::widget([
        'model' => $model,
        'attributes' => [
            'idTipoDado',
            'Tipo',
            'Descricao:ntext',
        ],
    ]);
    ?>

    <hr/>

    <?php
    echo Html::a('<span class="glyphicon glyphicon-trash"></span> Deletar', ['delete', 'idTipoDado' => $model->idTipoDado], [
        'class' => 'btn btn-danger',
        'data-confirm' => Yii::t('app', 'Tem certeza que quer deletar esse item?'),
        'data-method' => 'post',
    ]);
    ?>

    <?php $this->endBlock(); ?>



    <?php $this->beginBlock('Atributos'); ?>
    <p class='pull-right'>
        <?=
        \yii\helpers\Html::a(
                '<span class="glyphicon glyphicon-list"></span> Listar todos atributos', ['atributo/index'], ['class' => 'btn text-muted btn-xs']
        )
        ?>
        <?=
        \yii\helpers\Html::a(
                '<span class="glyphicon glyphicon-plus"></span> Novo Atributo', ['atributo/create', 'Atributo' => ['idTipoDado' => $model->idTipoDado]], ['class' => 'btn btn-success btn-xs']
        )
        ?>
    </p><div class='clearfix'></div>
    <?php Pjax::begin(['id' => 'pjax-Atributos', 'linkSelector' => '#pjax-Atributos ul.pagination a']) ?>
    <?=
    \yii\grid\GridView::widget([
        'dataProvider' => new \yii\data\ActiveDataProvider(['query' => $model->getAtributos(), 'pagination' => ['pageSize' => 10]]),
        'columns' => [ 'idAtributo',
            'Nome',
// generated by schmunk42\giiant\crud\providers\RelationProvider::columnFormat
            [
                "class" => yii\grid\DataColumn::className(),
                "attribute" => "idTipoAtributo",
                "value" => function($model)
                {
                    if ($rel = $model->getIdTipoAtributo0()->one())
                    {
                        return yii\helpers\Html::a($rel->label, ["tipo-atributo/view", 'idTipoAtributo' => $rel->idTipoAtributo,], ["data-pjax" => 0]);
                    } else
                    {
                        return '';
                    }
                },
                        "format" => "raw",
                    ],
                    'Descricao:ntext',
                    [
                        'class' => 'yii\grid\ActionColumn',
                        'template' => '{view} {update}',
                        'contentOptions' => ['nowrap' => 'nowrap'],
                        'urlCreator' => function($action, $model, $key, $index)
                {
                    // using the column name as key, not mapping to 'id' like the standard generator
                    $params = is_array($key) ? $key : [$model->primaryKey()[0] => (string) $key];
                    $params[0] = 'atributo' . '/' . $action;
                    return \yii\helpers\Url::toRoute($params);
                },
                        'buttons' => [
                        ],
                        'controller' => 'atributo'
                    ],]
            ]);
            ?>
            <?php Pjax::end() ?>
            <?php $this->endBlock() ?>


            <?=
            \yii\bootstrap\Tabs::widget(
                    [
                        'id' => 'relation-tabs',
                        'encodeLabels' => false,
                        'items' => [ [
                                'label' => '<span class="glyphicon glyphicon-asterisk"></span> TipoDado',
                                'content' => $this->blocks['app\models\TipoDado'],
                                'active' => true,
                            ], [
                                'label' => '<small><span class="glyphicon glyphicon-paperclip"></span> Atributos</small>',
                                'content' => $this->blocks['Atributos'],
                                'active' => false,
                            ],]
                    ]
            );
            ?></div>
